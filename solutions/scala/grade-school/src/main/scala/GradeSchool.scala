class School {
  private type DB = Map[Int, Seq[String]]
  private var database: DB = Map()

  def add(name: String, g: Int): Any = database = Map()

  def db: DB = database

  def grade(g: Int): Seq[String] = Seq()

  def sorted: DB = database
}

